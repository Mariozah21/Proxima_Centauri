{% extends '__smallmain.jinja' %}
{% from 'macros/form_field.jinja' import render_field with context %}

{% block content %}

<section>
    <div class="loginbox">
        <form method=post>
            <h1>Prihlásenie</h1>
            {{ render_field(form.login) }}
            <br>
            {{ render_field(form.heslo) }}
            <br>
            <input type="submit" class="login" value="Prihlásiť sa">
            <button onclick="window.location.href='/register'" class="login">Registrácia</button>
        </form>
    </div>
</section>

{% endblock%}